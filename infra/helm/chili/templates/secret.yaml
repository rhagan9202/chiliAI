{{/*
This template references an EXISTING Kubernetes Secret by name; it does not
create one. Provision the Secret out-of-band so values never touch git:

  kubectl create secret generic {{ .Values.secret.name }} \
    --from-literal=NEO4J_PASSWORD=... \
    --from-literal=QDRANT_API_KEY=... \
    --from-literal=MINIO_ACCESS_KEY=... \
    --from-literal=MINIO_SECRET_KEY=... \
    --from-literal=OPENAI_API_KEY=... \
    --from-literal=ANTHROPIC_API_KEY=... \
    --from-literal=REDIS_PASSWORD=... \
    --from-literal=JWT_SIGNING_KEY=...
*/}}
