# Template only — DO NOT commit real secret values.
#
# Provision the actual Secret object via:
#
#   kubectl create secret generic chili-secrets \
#     --from-literal=NEO4J_PASSWORD=changeme \
#     --from-literal=QDRANT_API_KEY=... \
#     --from-literal=MINIO_ACCESS_KEY=minioadmin \
#     --from-literal=MINIO_SECRET_KEY=minioadmin \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=ANTHROPIC_API_KEY=... \
#     --from-literal=REDIS_PASSWORD=... \
#     --from-literal=JWT_SIGNING_KEY=...
#
# This file is committed only as a reference for the expected key names.
apiVersion: v1
kind: Secret
metadata:
  name: chili-secrets
  labels:
    app.kubernetes.io/name: chili
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  NEO4J_PASSWORD: "REPLACE_ME"
  QDRANT_API_KEY: ""
  MINIO_ACCESS_KEY: "REPLACE_ME"
  MINIO_SECRET_KEY: "REPLACE_ME"
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  REDIS_PASSWORD: ""
  JWT_SIGNING_KEY: "REPLACE_ME"
