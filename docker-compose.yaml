services:
  # ---------- Application containers ----------

  app:
    build:
      context: ./chili_app
    ports:
      - "80:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - chili-net

  api:
    build:
      context: ./backend
    command: >
      uvicorn api.app:create_app
      --factory --host 0.0.0.0 --port 8000
      --workers ${API_WORKERS:-4}
    ports:
      - "${API_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      # Domain pack selector — must stay in lockstep with the worker service.
      - CHILI_CONFIG_PATH=${CHILI_CONFIG_PATH:-/app/config/defaults/medicare_fraud_cms_desynpuf.yaml}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - CHILI_EVENT_BUS_BACKEND=${CHILI_EVENT_BUS_BACKEND:-redis}
      - CHILI_EVENT_RECLAIM_MIN_IDLE_MS=${CHILI_EVENT_RECLAIM_MIN_IDLE_MS:-60000}
      - CHILI_WORKFLOW_RUN_STORE_BACKEND=${CHILI_WORKFLOW_RUN_STORE_BACKEND:-redis}
      # API and worker must agree so worker-persisted document metadata
      # (status, warning counts/reasons) is visible to API reads.
      - CHILI_KB_REPOSITORY_BACKEND=${CHILI_KB_REPOSITORY_BACKEND:-object_store}
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    restart: unless-stopped
    networks:
      - chili-net

  worker:
    build:
      context: ./backend
    command: python -m agent.coordinator
    env_file:
      - .env
    environment:
      # Must match the API service's CHILI_CONFIG_PATH — api and worker always
      # move to a new domain pack together.
      - CHILI_CONFIG_PATH=${CHILI_CONFIG_PATH:-/app/config/defaults/medicare_fraud_cms_desynpuf.yaml}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379}
      - CHILI_EVENT_BUS_BACKEND=${CHILI_EVENT_BUS_BACKEND:-redis}
      - CHILI_EVENT_RECLAIM_MIN_IDLE_MS=${CHILI_EVENT_RECLAIM_MIN_IDLE_MS:-60000}
      - CHILI_WORKFLOW_RUN_STORE_BACKEND=${CHILI_WORKFLOW_RUN_STORE_BACKEND:-redis}
      # API and worker must agree so worker-persisted document metadata
      # (status, warning counts/reasons) is visible to API reads.
      - CHILI_KB_REPOSITORY_BACKEND=${CHILI_KB_REPOSITORY_BACKEND:-object_store}
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    restart: unless-stopped
    networks:
      - chili-net

  # ---------- Infrastructure services ----------

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - chili-net

  neo4j:
    image: neo4j:5
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=${NEO4J_USER:-neo4j}/${NEO4J_PASSWORD:-changeme}
    volumes:
      - neo4j-data:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:7474 || exit 1"]
      interval: 15s
      timeout: 10s
      start_period: 30s
      retries: 5
    restart: unless-stopped
    networks:
      - chili-net

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/localhost/6333'"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - chili-net

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ACCESS_KEY:-minioadmin}
      - MINIO_ROOT_PASSWORD=${MINIO_SECRET_KEY:-minioadmin}
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - chili-net

volumes:
  redis-data:
  neo4j-data:
  qdrant-data:
  minio-data:

networks:
  chili-net:
    driver: bridge
