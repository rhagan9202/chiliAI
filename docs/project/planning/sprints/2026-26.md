# Sprint 2026-26 — Ingestion status visibility & hardening tail

> Owned by the Project Manager agent.
> Sprint: **2026-26** · Dates: **2026-07-13 → 2026-07-26** · Capacity: **25 SP (confirmed by product owner 2026-07-12)** · Theme: **make ingestion failures visible end-to-end, protect the schema, and retire a v1-required P1**

## Sprint inputs (user-confirmed 2026-07-12)

This plan **replaces the June draft** `2026-26-slice-ingestion-failure-path-observability-DRAFT.md` (now deleted); the draft's IFO-1..4 residue was re-verified against code on 2026-07-12 (IFO-1 ~90% done with a coordinator residue, IFO-3 core done, IFO-2/IFO-4 open — see Committed).

- **sprint-id**: `2026-26` — sequential after 2026-25, per the established convention (2026-23 explicitly chose sequence over ISO week; IDs and weeks diverged from 2026-24 on). Calendar weeks 26–28 had no sprint (domain-packs + af_housing ran outside cadence) but do not consume sequential IDs. Matches the June draft's own naming.
- **dates**: 2026-07-13 → 2026-07-26 (Mon→Sun). **Confirmed by product owner 2026-07-12.** Prior sprints ran Tue→Mon; the Monday start was proposed and accepted. Nominal only: every sprint since 2026-23 has delivered far ahead of its window.
- **capacity**: **25 SP** — held from 2026-24/25. **Confirmed by product owner 2026-07-12** (never re-baselined against merge-date actuals; the re-baseline followup carries forward). Committed core is deliberately 16 SP (precedent: 19/16/16/13), with the rest held as stretch + buffer.
- **focus**: land the residue of the June 2026-26 DRAFT slice (verified against code 2026-07-12), ship `database.04` alongside the slice's new migration, and finish **BL-019**, one of the two remaining v1-required P1s. The **BL-019 re-scope was ruled and approved 2026-07-12**: acceptance = embedding cache + cost/usage tracking; the roadmap-tier tail moves to post-v1 item **BL-045**.

## Goal

Every ingestion failure and empty extraction becomes visible and durable — a malformed document shows `FAILED` with a reason and a zero-entity document shows `EXTRACTED_EMPTY` in the per-KB document list, backed by a status projection whose migration is protected by a CI drift gate and instrumented with failure counters — while BL-019 closes the embeddings/vectorstore P1 hardening gap.

## Committed

| BL-ID | Module stories | REQ-IDs | Priority | Est. (verified 2026-07-12) | Owner | Acceptance Summary | Dependencies |
|---|---|---|---|---|---|---|---|
| **BL-041** *(new)* | ingestion.18 (slice) + ingestion.32 residue + ingestion.35 closure | REQ-KB-002, REQ-KB-004 | P1 | **6 SP** | TBD | `SourceDocumentStatusStore` protocol + Postgres adapter (migration `0009`; monotonic transitions — stale `parsing` after `failed` ignored); consumer projects `documents.uploaded/parsed/failed` + extraction-warning events; `GET /knowledgebases/{kb_id}/documents` returns durable `current_status`, `last_error`, drop counts/sample reasons, filterable by status; **coordinator residue**: `handle_documents_parsed/chunked` missing-key/`get_bytes` failures (`coordinator.py:1169-1172, 1234-1235`) converted to per-document `DocumentsFailedEvent` (one bad doc no longer poisons its batch); zero-valid-entity docs surface `EXTRACTED_EMPTY` via the projection (status transition — **no new event type**; registry untouched). Contracts regenerated (export OpenAPI → codegen). Frontend Studio wiring **explicitly out** (follow-on FE story). | — (database.04/events.04 verified non-blocking 2026-06-16, re-confirmed 2026-07-12) |
| **BL-042** *(new)* | database.04 | REQ-NFR-002 (quality-gate; protects REQ-KB-006/REQ-INT-004 persistence) | P1 (paired) | **3 SP** | TBD | `scripts/ci_migration_check.sh`: fresh TimescaleDB → `upgrade head` → `downgrade base` → `upgrade head` + drift check vs committed `snapshots/head.sql`; `make migrate-check` local parity; CI job wired (cross-edge `_cicd.12`); ships in the same sprint as migration `0009` so the new status table gets drift protection. Note: database.08–.13 doc revision-ID collisions renumber at implementation time (backlog carryover). | BL-041's migration (soft — gate lands with or after it) |
| **BL-043** *(new)* | ingestion.17 (logs+counters subset only) | REQ-NFR-SEC-004 (structured logs); counters are an enabler for post-v1 BL-034 | P2 | **2 SP** | TBD | Each ingestion stage emits structured logs (`stage=`, `source_document_id=`, `kb_id=`, `duration_ms=`, `outcome=`); counters `ingestion_documents_failed_total{stage,error_class}`, `ingestion_documents_empty_extraction_total`, `ingestion_dedup_suppressed_total` registered on the **existing** `/metrics` registry (`api/middleware/metrics.py` — verified present). **OTel spans + Grafana dashboards stay in ingestion.17** (blocked on _observability.03/.05/.07) — do not leak into this sprint. | BL-041 failure events (for the failure counter only; logs are day-1 independent) |
| **BL-019** | embeddings/vectorstore 1.0 remainder | REQ-VEC-001..004 | P1 | **5 SP** | TBD | Embedding cache (retire `embeddings/service.py` standing TODO) + cost/usage tracking shipped; the roadmap-tier tail (object-store embedding persistence, graph-metric hybrid flow, model routing, architecture guards) **re-scoped to post-v1 — ruled and approved by product owner 2026-07-12, tracked as BL-045** (REQ-VEC-001..004 do not require them; cache + tracking is the P1 substance). pyright --strict clean; pytest ≥ 85% on `embeddings/`/`vectorstore/`. | — (fully independent; day-1 parallel) |
| | **Committed core total** | | | **16 SP** | | | |

## Stretch

| BL-ID | Module story | REQ-IDs | Priority | Est. | Notes |
|---|---|---|---|---|---|
| **BL-044** *(new)* | config.04 | REQ-CONFIG-001 | P2 | 3 SP | Base + overlay config layering (`overlay.py`, `CHILI_CONFIG_OVERLAY_PATH`, merge ADR, `medicare_fraud_dev.yaml` shrunk to an overlay). The ingestion roadmap's named next lever — unblocks ingestion.08/09/13/15 + agent.05/10 + config.08. Pull only if core is green at the mid-sprint checkpoint. |

Remaining ~6 SP is buffer, plus two ≤1 SP buffer hygiene tasks: (1) charter `docs/backlog/scorecards.md` so `backend/scorecards/` hardening stories have a home (flagged carryover, module-backlog owner); (2) author the **BL-017 graph-integrity design note** (merge semantics, version-conflict strategy) so BL-017 can headline 2026-27 at a trustworthy 8 SP.

## Dependency order

```
BL-041 coordinator-residue (failure events) ──▶ BL-041 projection/endpoint ──▶ EXTRACTED_EMPTY surfacing
                                   └──────────▶ BL-043 failure counter
BL-042 (migration gate) ── lands with/after BL-041's 0009 migration
BL-019 ── fully independent, day 1
BL-043 logs ── independent, day 1
BL-044 (stretch) ── only after core checkpoint
```

## Risks & Dependencies

- **R-1 — BL-041 is the cost center and critical path.** Status schema + monotonic-transition consumer + endpoint rework in one story. Mitigation: land the coordinator failure-event residue first (small, unblocks everything); model `EXTRACTED_EMPTY` as a status transition, not a new event (hand-maintained codec registry untouched); if it slips, BL-019/BL-042/BL-043-logs still ship standalone value.
- **R-2 — BL-042 CI flakiness.** Fresh-DB-in-CI gates can flake (container startup, timing). Mitigation: reuse the CI service-container pattern already proven by the existing `alembic upgrade head` step at `ci.yml:87`; drift check via committed `head.sql` snapshot, not live autogenerate.
- **R-3 — BL-019 scope ambiguity.** "Hardening remainder" spans six unlike items. Mitigation applied: acceptance is pinned to cache + cost/usage tracking; the re-scope of the roadmap tail to post-v1 was **ruled and approved 2026-07-12** and is tracked as BL-045 (nothing silently dropped).
- **R-4 — Capacity + dates were assumptions, now confirmed.** 25 SP and the 07-13 → 07-26 window were confirmed by the product owner 2026-07-12; the merge-date throughput re-baseline still carries forward as a followup. Core is only 16 SP with ~9 SP stretch/buffer.
- **R-5 — Estimate drift is systemic** (2026-25 retro). Mitigation applied: every committed story above was code-verified on 2026-07-12, not taken from backlog labels.

## Exit Criteria

- Ingest a malformed doc → it shows `FAILED` with a reason in `GET /knowledgebases/{kb_id}/documents`; ingest a zero-entity doc → it shows `EXTRACTED_EMPTY`, not a silent ready; a coordinator missing-key failure produces a per-document `DocumentsFailedEvent` and does not poison its batch (manual + automated verification on the full stack).
- `/metrics` exposes the three new ingestion counters; stage logs carry `stage=/duration_ms=/outcome=` fields.
- `make migrate-check` green locally and in CI, covering migration `0009`; `snapshots/head.sql` committed.
- Embedding cache + cost/usage tracking shipped; BL-019 flipped `done` (with the post-v1 re-scope recorded as BL-045) — leaving **BL-017 as the sole remaining P1**.
- Gates: pyright --strict clean, pytest ≥ 85% on touched packages, full green; ESLint clean/TS strict for any touched frontend; frontend-consumed contract changes regenerated (no drift).
- Backlog updated: ingestion.18 → `done (sliced; FE wiring split out)`, ingestion.35 → `done`, ingestion.17 annotated "logs+counters delivered; OTel+Grafana remain", ingestion.32 annotated with the coordinator-residue closure; database.04 → `done`; BL-041..045 reflected in `docs/project/planning/backlog.md`.

## Drift Resolutions Carried From Phase 3

- **None open.** Requirements v1.2 (2026-07-12) closed all prior drift and [NO-REQ] items: backlog v3 is code-verified against prod with zero unresolved drift; BL-038/BL-039 gained REQ-SCORE-*/REQ-HOUSING-* families; BL-016 closed at its v1 bar (post-v1 tail → BL-040); the config audit-trail gap is a documented accepted deviation (requirements §6).

## Progress (2026-07-13)

- **BL-041 — code-complete.** All 10 implementation tasks (status models/rank map, in-memory + Postgres `SourceDocumentStatusStore` adapters, migration `0009_document_status`, worker `_dispatch_event` → `agent/status_projection.py` wiring, KB-delete cascade step, `GET /knowledgebases/{kb_id}/documents` durable fields + `?status=` filter, document-delete/reupload purge, contracts regeneration, this docs-reconciliation task) landed with clean per-task reviews. Backend gates green in-session: `pytest --cov -m "not integration"` (2337 passed, 1 skipped, 59 integration deselected, 94% aggregate coverage), `pyright` (0 errors), `ruff check` (clean). **Not yet run**: full-stack manual verification (malformed doc → `FAILED`; zero-entity doc → `EXTRACTED_EMPTY`; coordinator missing-key failure isolation) and `DATABASE_URL=... pytest -m integration tests/database` — the dev-stack Docker environment was unavailable this session (container networking issue, tracked separately). BL-041 stays **not** flipped to Done pending that verification pass.
- **BL-043 — code-complete (2026-07-14).** All 6 implementation tasks landed: `shared/metrics.py` (three counters + `log_stage` on logger `chili.ingestion.stage`), worker `GET /metrics` on the health server (`agent/health.py`, port 8001), parse-stage log + failure counter (`ingestion/service.py`), chunk/extract/validate stage logs + empty-extraction counter (`agent/coordinator.py` — including failure-counter increments at all four BL-041 per-document `DocumentsFailedEvent` sites, a controller addition beyond the original plan text), dedup counter at both suppression points (`records/service.py` kind=`record_batch`, `ingestion/service.py` kind=`document`), and this docs task. Backend gates green in-session: `pytest --cov -m "not integration"` full pass (2351 passed, 1 skipped, 59 deselected, 94% aggregate), coverage `shared` 97%/`ingestion` 93%/`records` 85%/`agent` 92% (all ≥ 85%), `pyright` (0 errors), `ruff check --no-cache` (clean). **Not yet run**: live scrape of `GET :8001/metrics` (worker) and `GET :8000/metrics` (API) against the running `make dev` stack, and worker log inspection for `ingestion_stage` lines — Docker was unavailable this session; tracked as a follow-up verification pass, not additional scope. BL-043 stays **not** flipped to Done pending that verification pass. `ingestion.17` annotated in `docs/backlog/ingestion.md` per the exit-criteria convention: "logs+counters delivered; OTel+Grafana remain".
- **BL-019 — code-complete (2026-07-14).** All 8 implementation tasks landed: `EmbeddingsConfig` cache knobs (+ contracts regen), `CachedEmbedding` + `build_embedding_cache_key`, `EmbeddingCacheProtocol` + `InMemoryLruEmbeddingCache` + `create_embedding_cache`/`embedding_cache_namespace`, cache-aware `EmbeddingsService` (standing TODO at `embeddings/service.py:24` retired), OpenAI usage capture (`EmbeddingMetadata.total_tokens`), `embeddings/metrics.py` (`embedding_requests_total`, `embedding_texts_total{cache_result}`, `embedding_tokens_total{source}`) + structured usage log, DI wiring at both the API and worker composition roots with hot-swap safety, and this docs task (including an `__all__`-ordering hygiene fix in `embeddings/models.py`/`embeddings/__init__.py`). Backend gates green in-session: `pytest --cov -m "not integration"` full pass, `embeddings`/`vectorstore` both ≥ 85% coverage, `pyright` (0 errors), `ruff check --no-cache` (clean), contract export (`chili_app/openapi.json`) zero-diff. **Not yet run**: full-stack manual verification (cache hit/miss behavior against a running stack, `/metrics` scrape, structured usage-log inspection) — Docker was unavailable this session; tracked as a follow-up verification pass, not additional scope. BL-019 stays **not** flipped to Done pending that verification pass. `embeddings.02`/`.04`/`.05` annotated in `docs/backlog/embeddings.md` per the exit-criteria convention with what shipped vs. what moved to BL-045 (Redis cache adapter, OTel spans/histograms, per-tenant USD cost attribution).
- BL-042/BL-044 unaffected by this pass; still as committed/stretch above.

## Progress (2026-07-14 — deferred verification pass)

The follow-up verification pass deferred from the 2026-07-13/14 sessions (Docker unavailable) ran against a full `make dev` stack plus a dispatched CI run. Results:

- **BL-042 — done.** The `migrations` CI job's first real run **failed**: `backend/database/migrations/snapshots/head.sql` was planned as a committed artifact (plan Task list: "Create (generated)") but was never generated. Generated via `make migrate-snapshot`, `make migrate-check` green locally, snapshot committed — the dispatched CI run is now fully green (all 5 jobs, `migrations` in 1m0s). `database.04` flipped `done`.
- **BL-041 — done, with a real bug found and fixed.** Individually: malformed PDF → `current_status=failed` + `last_error` ("Unable to read PDF content…"), zero-entity doc → `extracted_empty`, `?status=` filter exact, and `DATABASE_URL=…chili_test pytest -m integration tests/database` → 22 passed. But a **mixed batch** (malformed + healthy doc in one upload) still poisoned the batch: the per-document `documents.failed` marked the whole workflow run `FAILED`, and `WorkflowEventTracker.begin_event` then skipped the healthy sibling's `documents.chunked` ("Skipping terminal workflow event"), stranding it at `parsed` forever. Fix: a `FAILED` run no longer gates sibling event processing (run record stays frozen at `FAILED`; `CANCELLED`/`COMPLETED` still gate) — `agent/workflow_tracking.py` + regression tests. Re-verified live: the same mixed batch now ends `failed` + `extracted_empty` with the full parse→chunk→extract→validate stage-log trail.
- **BL-043 — done.** Worker `GET :8001/metrics` (host) shows `ingestion_documents_failed_total{stage="parse",error_class="ParserError"}` and `ingestion_documents_empty_extraction_total`; API `GET :8000/metrics` shows `ingestion_dedup_suppressed_total{kind="document"}` on re-upload; `ingestion_stage` logs carry all five fields across parse/chunk/extract/validate. One gap fixed: the worker's health/metrics port was never published to the host — `8001:8001` added to `docker-compose.dev.yaml` so the documented scrape works as written.
- **BL-019 — done.** Identical chat question twice: first `embedding_texts_total{cache_result="miss"}=1`, `embedding_tokens_total…=13` (`source="estimated"`); second `{cache_result="hit"}=1` with tokens **flat** (fully cached call spends zero tokens). Usage log: `cache_misses=1 tokens=13 token_source=estimated` then `cache_hits=1 tokens=0 token_source=cached`.
- Gates after the tracker fix: `pytest --cov -m "not integration"` 2391 passed / 94% aggregate, `pyright` 0 errors, `ruff check --no-cache` clean.
- Sprint exit criteria for BL-019/BL-041/BL-042/BL-043 are met; **BL-017 is now the sole remaining v1-required P1**. BL-044 (stretch) untouched.
