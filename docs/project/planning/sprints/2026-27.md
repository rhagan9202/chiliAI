# Sprint 2026-27 — P1 closeout & platform levers

> Owned by the Project Manager agent.
> Sprint: **2026-27** · Dates: **2026-07-15 → 2026-07-28** · Capacity: **25 SP (held from prior sprints; plan approved by product owner 2026-07-14)** · Theme: **retire the final v1-required P1 (graph integrity), ship the config-overlay lever, and close out event replay**

## Sprint inputs (user-confirmed 2026-07-14)

- **sprint-id**: `2026-27` — sequential after 2026-26, per the established convention (IDs and calendar weeks diverged from 2026-24 on).
- **dates**: 2026-07-15 → 2026-07-28 (Wed→Tue). **Confirmed by product owner 2026-07-14** — start immediately rather than waiting out 2026-26's nominal window (07-26): 2026-26 delivered and live-verified in 2 days, continuing the ahead-of-window precedent held since 2026-23.
- **capacity**: **25 SP** — held from 2026-24/25/26. Committed core is deliberately **14 SP** (delivered-core precedent: 19/16/16/13/16), with ~4 SP stretch and ~7 SP buffer.
- **focus** (user-chosen 2026-07-14 from three proposed compositions): **"P1 closeout + levers"** — BL-017 graph integrity (the sole remaining v1-required P1), BL-044 config overlay (the ingestion roadmap's named next lever, un-pulled stretch in 2026-26), and BL-023 event-replay closeout (a half-shipped DR item). The alternatives (a user-visible multi-KB-RAG composition and a DR-themed sweep) were declined for this sprint.

## Goal

Close the v1-required book: relationship writes can no longer create phantom graph endpoints or silently clobber concurrent versions, the config system gains base+overlay layering (unblocking ingestion.08/09/13/15, agent.05/10, config.08), and a dead-lettered event can be durably stored, inspected, and re-driven by an operator. After this sprint, the active backlog is pure P2 hardening/DR.

## Verified predecessor state (code-checked 2026-07-14)

- **BL-017 (graph)** — essentially unmoved since 2026-06-16, re-verified today: `graph/service.py:29` carries the idempotency/version TODO; `in_memory.py:21-22` carries the referential-integrity TODO and `upsert_relationships` (`:41-50`) writes unchecked; Neo4j `MERGE` silently creates phantom endpoint nodes (`neo4j_adapter.py:226-228`); `version` is stored and blindly overwritten (`neo4j_adapter.py:176,188`); the only merge is a property-dict overwrite on single-entity update.
- **BL-044 (config)** — no `backend/config/overlay.py`, no `CHILI_CONFIG_OVERLAY_PATH`, loader TODO intact at `config/loader.py:31`. Unchanged since the 2026-07-12 verification.
- **BL-023 (events)** — 3 of 6 acceptance items shipped: `xautoclaim` stale-pending reclaim, DLQ stream publish, and the handler retry/DLQ wrapper. **Missing**: durable DLQ persistence (no table/migration), any replay API/operator script, and an operator runbook (`docs/runbooks/` does not exist).
- **BL-022 (stretch; OIDC)** — 4 of 7 acceptance items shipped: JWKS TTL cache, full aud/iss/exp+signature validation, validated id_token decode in the callback, production auth guardrail. **Missing**: kid-aware forced JWKS refresh on unknown `kid` + rotation tests, nonce generation/validation (PKCE `state` only today), IdP configuration templates.
- **Sprint 2026-26** — all four stories done and live-verified 2026-07-14 (see `2026-26.md` § Progress); branch `feat/sprint-2026-26-ingestion-visibility` not yet merged to prod (prerequisite 1 below).

## Committed

| BL-ID | Module stories | REQ-IDs | Priority | Est. (verified 2026-07-14) | Owner | Acceptance Summary | Dependencies |
|---|---|---|---|---|---|---|---|
| **BL-017** | graph.01 + graph.02 | REQ-GRAPH-001 | P1 | **8 SP** | TBD | Referential integrity: relationship writes validate endpoint existence in **both** adapters — a relationship referencing a missing entity is rejected, not silently satisfied by a phantom `MERGE` node. Optimistic locking: version-conflict detection replaces blind `version` overwrite; a stale-version write surfaces a typed conflict. Real merge semantics per the pre-sprint design note (scope pinned to REQ-GRAPH-001's bar — no speculative CRDT/history machinery). pyright --strict clean; pytest ≥ 85% on `graph/`; Neo4j paths integration-tested against the live stack. | Design note (prerequisite 2) is a **hard gate** before implementation starts |
| **BL-044** | config.04 | REQ-CONFIG-001 | P2 | **3 SP** | TBD | Merge-semantics ADR; `backend/config/overlay.py` with property-based merge tests; stackable `CHILI_CONFIG_OVERLAY_PATH`; `medicare_fraud_dev.yaml` shrunk to a minimal overlay over the base pack; unknown-top-level-key rejection. Must respect pointer-overrides-env precedence and hot-swap (see R-3). Unblocks ingestion.08/09/13/15 + agent.05/10 + config.08. | — (independent, day 1) |
| **BL-023** | events replay closeout | REQ-WORKFLOW-005, REQ-NFR-DR-003 | P2 | **3 SP** | TBD | Durable DLQ persistence (table + Alembic migration — refresh `snapshots/head.sql` per the BL-042 standing rule); a replay surface (API route or operator script) that re-drives a dead-lettered event through the normal dispatch path; operator runbook under `docs/runbooks/`. | — (independent, day 1) |
| | **Committed core total** | | | **14 SP** | | | |

## Stretch

| BL-ID | Module story | REQ-IDs | Priority | Est. | Notes |
|---|---|---|---|---|---|
| **BL-022** | _security.01 remainder | REQ-AUTH-001 | P2 | ~4 SP | **Done 2026-07-15.** kid-aware key rotation (forced JWKS refresh on unknown `kid` + rotation tests), nonce generation/validation in the OIDC flow, IdP configuration templates (Keycloak/Okta). Pulled after the 14 SP core completed day 1; see § Progress (2026-07-15). |

Remaining ~7 SP is buffer, plus the carried ≤1 SP hygiene task: charter `docs/backlog/scorecards.md` so `backend/scorecards/` hardening stories have a home (carried from 2026-26's unexecuted buffer).

## Pre-sprint prerequisites

1. **PR + merge `feat/sprint-2026-26-ingestion-visibility` to prod.** Sprint 2026-26 exit criteria were met and live-verified 2026-07-14; the branch carries the workflow-tracker isolation fix and the CI migration snapshot that 2026-27's work builds on.
2. **BL-017 design spec + impl plan** (`docs/superpowers/specs/` / `docs/superpowers/plans/`): merge semantics, version-conflict strategy, and the endpoint-existence-check batching approach. Carried from 2026-26's unexecuted buffer; a **hard gate** before BL-017 implementation — the 8 SP is only trustworthy with the semantics pinned.
3. **BL-044 and BL-023 specs/plans** — small, per the spec→plan convention.

## Dependency order

```
BL-017 ── independent, day 1 (largest; start first) — gated on its design note
BL-044 ── independent, day 1
BL-023 ── independent, day 1
BL-022 (stretch) ── only after the mid-sprint core checkpoint
```

No cross-story dependencies: all three core stories touch disjoint modules (`graph/`, `config/`, `events/`+`database/`).

## Risks & Dependencies

- **R-1 — BL-017 is the cost center.** Version/merge semantics can rabbit-hole into distributed-systems territory. Mitigation: the design note (prerequisite 2) pins scope to REQ-GRAPH-001's bar **before** code starts; anything beyond (multi-writer CRDTs, full audit history) is named and deferred, not silently attempted.
- **R-2 — Integrity checks can regress ingestion throughput.** Existence validation on every relationship write adds round-trips on the hot ingestion path. Mitigation: batch the existence checks per `upsert_relationships` call; measure before/after with BL-043's `ingestion_stage` `duration_ms` logs — a regression is now visible, not anecdotal.
- **R-3 — BL-044 merge rules interact with domain-pack precedence.** The pack system has a known pointer-overrides-env precedence gotcha, and hot-swap cannot change the event transport. Mitigation: the merge ADR states precedence explicitly (base ← overlay ← env ← pointer) and the property-based tests cover the documented order; overlay loading follows the same guardrails as pack hot-swap.
- **R-4 — BL-023's migration risk is now fenced.** Any new DLQ table must refresh `snapshots/head.sql`; the BL-042 CI gate fails fast on a miss (proven live 2026-07-14 — the gate's first catch was exactly a missing snapshot).
- **R-5 — Estimate drift is systemic** (2026-25 retro; reaffirmed by 2026-26's verification pass finding two real defects in "code-complete" work). Mitigation: every committed story above was code-verified 2026-07-14, and **"code-complete, verification deferred" is treated as unproven** — each story's DoD includes its live verification in-sprint, not as a deferred pass.

## Exit Criteria

- A relationship write referencing a missing endpoint entity is **rejected** in both the in-memory and Neo4j adapters (no phantom nodes); a stale-version write surfaces a version conflict; merge behavior matches the design note — verified against the live stack, not only unit tests.
- The dev stack boots with `medicare_fraud_dev.yaml` as a minimal overlay over the base pack via `CHILI_CONFIG_OVERLAY_PATH`; unknown top-level keys are rejected; property-based merge tests green.
- A dead-lettered event is visible in durable storage and successfully re-driven through the pipeline via the replay surface on the live stack; runbook exists under `docs/runbooks/`.
- Any new migration lands with a refreshed `snapshots/head.sql` (CI `migrations` job green).
- Gates: pyright --strict clean, pytest ≥ 85% on touched packages, full green; ESLint clean/TS strict for any touched frontend; frontend-consumed contract changes regenerated (no drift).
- Backlog updated: graph.01/graph.02 → `done`, config.04 → `done`, events replay stories annotated; BL-017/BL-022/BL-023/BL-044 statuses reflected in `docs/project/planning/backlog.md`.
- **Live verification happens in-sprint per story** — no deferred verification pass (R-5).

## Progress (2026-07-14)

- **BL-017 — code-complete.** All 7 implementation tasks landed on `feat/sprint-2026-27-graph-integrity`: `GraphIntegrityError`/`GraphVersionConflictError` + `GraphUpsertOptions` (Task 1), `InMemoryGraphRepository` entity merge/version (Task 2), `InMemoryGraphRepository` relationship strict-endpoint integrity (Task 3), `Neo4jGraphRepository` entity atomic read-modify-write merge/version (Task 4), `Neo4jGraphRepository` relationship strict-endpoint integrity + merge/version (Task 5), `GraphBuildTask.upsert_options` service/records plumbing (Task 6), and `agent.coordinator.handle_entities_validated` per-document isolation for `GraphIntegrityError`-caused `BatchUpsertError` failures plus this docs closeout (Task 7). `graph/service.py`'s standing idempotency TODO is retired (version-conflict detection shipped; remaining skip-publish-on-unchanged change detection is tracked separately as `graph.03`). Backend gates green in-session: `pytest tests/agent tests/graph -q` (401 passed, 1 skipped), full-suite `pytest --cov -m "not integration"` (2416 passed, 1 skipped, 60 deselected, 94% aggregate), `graph` package coverage 89% (≥ 85%), `pyright` (0 errors), `ruff check --no-cache` (clean). `docs/backlog/graph.md` graph.01/graph.02 flipped to `done` with two deliberate deviations recorded (`integrity_mode` on `GraphUpsertOptions` rather than a separate `GraphService` kwarg; `merge_properties` is shallow, not deep). **Live verification passed same-day (2026-07-14, in-session per R-5)** against the full `make dev` stack: a crafted two-document `entities.validated` batch (dangling-endpoint document FIRST) proved per-document isolation live — `stage=graph outcome=failed` for the bad document, `outcome=success` for its sibling in the same batch, `ingestion_documents_failed_total{stage="graph",error_class="GraphIntegrityError"}=1` scraped from worker `:8001/metrics`, a durable `failed` projection row carrying "relationships reference missing entities ['beneficiary:GHOST']", and zero phantom nodes in Neo4j. The sibling's re-upsert of the live claim entity proved shallow merge (changed key overwritten, untouched keys preserved) with version 1→2, and an identical replay of the same event left version and properties byte-stable (no bump on no-op). Records path exercised via two `carrier_claims_a` pushes (entities + `billed_for`/`submitted_by` relationships written strict-mode, all version 1). **BL-017 fully done.** Follow-up recorded (pre-existing, out of scope): records per-row dedup (`ON CONFLICT (kb, record_type, record_id) DO NOTHING`) silently drops changed-content re-pushes of an existing record_id — zero counters increment and no receipt field reflects it; worth an observability story alongside BL-034.
- BL-023/BL-022 unaffected by this pass; still as committed/stretch above.

## Progress (2026-07-15)

- **BL-044 — code-complete.** All 5 implementation tasks landed on `feat/sprint-2026-27-config-overlay`: pure merge + hypothesis property tests over type-stable layer stacks plus a deterministic type-flip pin (Task 1), `apply_overlays` with the `overlay_for` skip-with-warning guard and unknown-top-level-key rejection (Task 2), `CHILI_CONFIG_OVERLAY_PATH` wired into every `load_config` call path (comma-separated, declared order, last wins) (Task 3), `medicare_fraud_dev.yaml` moved from `config/defaults/` to `config/overlays/` and rewritten as a 115-line overlay (was 284 lines — **59% reduction**, not the ~80% originally estimated, because list-replace semantics force the whole `policy_rules` block to be restated) with a golden equivalence test against a checked-in snapshot fixture (Task 4), and this ADR + docs + backlog closeout (Task 5). `docs/architecture/decisions/0001-config-overlay-merge-semantics.md` is the repo's first ADR; it records the amended algebra (associativity evidenced by property tests only on type-stable stacks — the original unrestricted-associativity claim was falsified by a Task-1 review counterexample and the type-flip case is pinned to left-to-right application-order semantics). `docs/backlog/config.md` config.04 flipped to `done` with the reduction and pack-catalog-separation annotations recorded. Backend gates green in-session: full-suite `pytest --cov -m "not integration"`, `pyright` (0 errors), `ruff check --no-cache .` (clean); `config` package coverage ≥ 85%. **Live verification passed same-day (2026-07-15, in-session per R-5)**: with a `CHILI_CONFIG_OVERLAY_PATH` passthrough added to the dev compose (api+worker — the documented workflow was otherwise not exercisable in-stack), booting on `medicare_fraud.yaml` + the dev overlay served the merged config (`monitoring.evaluation_interval_seconds=300` from the overlay-only section, `capabilities.peer_stats=false`, `facility` display card); `GET /config/packs` no longer lists the retired dev pseudo-pack; hot-swapping to `department_air_force_housing` with the overlay env still set logged the exact ADR-cited skip warning twice (api boot + worker rebuild) and served clean housing config with zero medicare rule leakage. **BL-044 fully done.**
- **BL-023 — code-complete.** All 5 implementation/docs tasks landed on `feat/sprint-2026-27-event-replay`: `DlqRecord`/`DlqRecordStore` protocol + `InMemoryDlqRecordStore` (Task 1), `PostgresDlqRecordStore` + migration `0010_event_dlq` + `snapshots/head.sql` refresh (Task 2), retry-wrapper persistence (`run_handler_with_retry` best-effort `DlqRecord` persist on retry exhaustion) + worker wiring (`build_dlq_record_store`, `WorkerDependencies.dlq_record_store`) + a terminal-state persist guard added at review (a `replayed`/`discarded` record can never be silently reverted to `pending` by a later persist, on both adapters) (Task 3), the `/events/dlq` operator API (`GET` list/detail, `admin`-gated `POST .../replay` and `POST .../discard`) + contracts regen (Task 4), and this runbook/module-README/backlog closeout pass (Task 5). `docs/backlog/events.md` events.10 flipped to `done` with the redaction AC deliberately skipped as a documented deviation (no existing redaction conventions anywhere in the repo; payloads are reference-shaped by construction) and "linked to original event IDs" satisfied via `correlation_id` (the Redis stream message id is unavailable at the wrapper, since delivery is already ACKed/exhausted by the time retries are spent). `docs/runbooks/event-replay.md` is the repo's first operator runbook — it documents the accepted publish-before-CAS concurrency tradeoff (a race between two operator actions on the same record can double-publish the underlying event but never lose it, safe because downstream handlers are idempotent by construction per BL-041/BL-017) and the `event_dlq`-table-vs-`.dlq`-Redis-stream relationship. Backend gates green in-session: full-suite `pytest --cov -m "not integration"`, `pyright` (0 errors), `ruff check --no-cache .` (clean); `events` package coverage ≥ 85% (`dlq_postgres.py` itself at 84%, matching the `ingestion/adapters/postgres.py` exemplar's DB-failure-branch coverage ceiling). **Live-stack verification against `make dev` passed 2026-07-15**: a forced poison event produced a durable `pending` `DlqRecord` (`retry_count: 3`, full traceback) via `GET /events/dlq`; replaying it while still broken transitioned the original to `replayed`, re-drove the event, and produced a new pending record (repeat replay of that record correctly `409`'d); discarding it transitioned it to `discarded` (repeat discard `409`'d); fixing the cause and replaying again drove the pipeline to completion (`stage=graph outcome=success`, zero pending records remaining). Role gates were verified in unit tests with auth enabled — the live dev stack itself runs auth-disabled by design, so `require_role` short-circuits open there (`api/middleware/rbac.py:66-67`); the runbook auth-posture note was corrected accordingly (it previously implied `CHILI_DEV_ANONYMOUS_ROLE` gates dev mutations, which is not how `require_role` behaves when the pack has no auth enabled). **Ops lesson recorded:** `docker compose up -d` is a no-op when env is unchanged — a stale worker process served pre-BL-023 code until an explicit `restart`; worth remembering for any volume-mounted deploy. **BL-023 fully done.**
- **BL-022 — done (Sprint 2026-27 stretch delivered).** Pulled after the 14 SP core (BL-017/BL-044/BL-023) completed day 1, per the stretch table's gate. All 4 implementation/docs tasks landed on `feat/sprint-2026-27-oidc-hardening`: `JwksCache.invalidate_uri` + a throttled `force_refresh` (Task 1), kid-aware `decode_token` with a rotation test matrix (old-key validates, new-key triggers exactly one forced refetch and validates, old-key then 401s, a repeat unknown-`kid` inside the 30s throttle window does not refetch again) (Task 2), OIDC `nonce` round-trip via a typed `PkceState` (both session-store adapters) validated against the decoded `id_token` (Task 3), and worked Keycloak/Okta IdP templates (`docs/auth/idp-templates.md`) plus this closeout pass (Task 4). `docs/backlog/_security.md` story _security.01 flipped to `done` with two recorded deviations: the templates are desk-checked against IdP documentation only, not verified against a live IdP (product-owner ruling, 2026-07-15 — **BL-022's stretch scope is test-double-verified only; there is no deferred live-IdP verification pass owed by this story**, only a recorded residual follow-up, see below); and nonce validation applies to id_token flows only (id_token-only OIDC-spec claim — the access-token fallback path has no nonce to check and skips it by design, not silently). Backend gates green in-session: full-suite `pytest --cov -m "not integration"`, `pyright` (0 errors), `ruff check --no-cache .` (clean); `api` package coverage ≥ 85%. **BL-022 fully done — this closes the last open story of Sprint 2026-27**: committed core (BL-017, BL-044, BL-023) and the BL-022 stretch are all done, with the ~7 SP buffer untouched.

## Deferred / next up

- **BL-028 multi-KB RAG scope (5 SP)** — the user-visible capability composition was considered and declined for 2026-27; strongest candidate for a 2026-28 headline alongside the DR band.
- **DR band (BL-020 KB snapshots 8 SP, BL-021 graph backup/restore 5 SP)** — natural 2026-28 theme once BL-023 lands the replay half.
- **BL-024 load testing (8 SP), BL-027 per-KB ACL (8 SP)** — remain the deepest P2 items; BL-024 also unblocks the four open `[ASSUMPTION]` requirement items.
- **Live-IdP template verification (BL-022 residual follow-up)** — `docs/auth/idp-templates.md`'s Keycloak/Okta templates are desk-checked against IdP documentation only (product-owner ruling, 2026-07-15: BL-022's stretch scope is test-double-verified, not live-IdP-verified). Standing up a real Keycloak or Okta tenant, running the BFF flow end-to-end, and forcing a live key rotation is recorded here as the residual follow-up, not as a deferred pass owed by the closed story.
- Capacity re-baseline against merge-date actuals — followup carried again.

### BL-017 final-review fix-later notes (recorded 2026-07-15, none merge-blocking)

- **Relationship `metadata` not persisted in Neo4j** (pre-existing: `_relationship_row` has no `metadata_json` column) — BL-017 made metadata semantics load-bearing (in-memory merges and round-trips it; Neo4j silently drops it). Highest-value item: needs a `graph.md` module story before anyone relies on relationship metadata.
- `updated_at` stamping divergence: in-memory stamps `utc_now()` on write-through, Neo4j writes the payload value (possibly `None`) verbatim — nothing consumes `Entity.updated_at` today; fold into the same parity story.
- When `expected_version` gets armed by a future caller, extend `handle_entities_validated`'s permanent-cause set to include `GraphVersionConflictError`.
- Records per-row dedup silently drops changed-content re-pushes of an existing `record_id` (zero counters; pre-existing BL-015 design) — observability story alongside BL-034.
- Micro/polish: intra-batch duplicate-entity-id merge divergence Neo4j vs in-memory (latent; upstream dedup guards — resolve at graph.03/graph.04); hoist `set(missing)` in Neo4j integrity check; skip double `json.dumps` on Neo4j update path; optionally filter unchanged rows from the Neo4j UNWIND payload; mixed match/conflict batch-atomicity test variant; in-memory adjacency-stale marking on no-op batches.

### BL-044 final-review fix-later notes (recorded 2026-07-15, none merge-blocking)

- ~~**Overlay guard is domain-scoped, not pack-scoped**~~ — **RESOLVED 2026-07-16 (sprint 2026-27 fix-later burn-down, Task 3)**: per product-owner re-ruling 2026-07-15, `overlay_for` now matches the base pack's filename stem (`apply_overlays(..., base_path=...)`), not `domain.name`; ADR 0001 amended; `medicare_fraud.yaml` and `medicare_fraud_cms_desynpuf.yaml` no longer share an overlay despite sharing `domain.name: medicare_fraud`.
- Known-keys test is no longer tautological — replaced with a test that loads the shipped `overlays/medicare_fraud_dev.yaml` through the real unknown-key guard (passes) and a canary-typo variant of it (rejected naming the key), in addition to the existing golden equivalence test.
- **`POST /config/validate` is an overlay-free dry run** while `apply`/`switch` activate the merged config — dry-run verdict can differ from the served result (safe: apply revalidates merged before mutating; documented in `backend/config/README.md`).
- **`config` package is not yet in pyright's strict `include` scope** (`backend/pyproject.toml`'s `tool.pyright.include` has no `config` entry — the bare `pyright` gate only enforces strict mode on files it lists). `backend/config/overlay.py` and `backend/tests/config/test_overlay.py` are independently strict-clean (`pyright config/overlay.py tests/config/test_overlay.py` → 0 errors), but a pre-existing strict error already exists elsewhere in the package: `backend/config/schema.py:682` (`reportUnknownVariableType` on `ScorecardsConfig.templates`). Hardening the whole `config` package (schema.py, loader.py, store.py, overlay.py, and their tests) into the strict include scope is follow-up work, not part of BL-044/config.04.

### BL-022 final-review fix-later notes (recorded 2026-07-15, none merge-blocking)

- **Live-IdP verification is outstanding, by design** — see § Deferred / next up. Not a gap in this closeout; the product-owner ruling accepted test-double verification as this stretch's bar.
- **IdP template scope is Keycloak + Okta only** (design-note §5 scope guard, product-owner-approved) — Auth0, Cognito, and Google Workspace templates from the original `_security.01` acceptance-criteria wording are not covered. Add them as a follow-up story if a deployment needs one; `docs/auth/idp-templates.md` documents the schema-fields-first approach to extend.
- **No `chili_jwks_*` Prometheus metric was added** for forced-refetch events — the original AC wording mentioned a refetch metric; rotation is proven via test-double fetch-count assertions instead (per the test-double-only ruling), with no operational counter to alert on in production. Worth an observability follow-up alongside BL-034 if kid-rotation frequency needs monitoring in practice.
- **Refresh-token rotation and back-channel logout remain out of scope** (explicitly named in the design note §5) — tracked separately as `_security.08` (refresh-token rotation/session revocation); back-channel logout has no dedicated backlog item yet.
- **`AuthConfig.jwks_cache_seconds` is dead config** — nothing wires it into the process-wide JwksCache (hardcoded 3600). Wire it at cache construction or drop it from the schema + templates.
- **IdP returning 200 with a body missing `access_token` raises an uncaught ValidationError** → 500 in the callback (no session minted; pre-existing). Wrap as a 400 when next touching the flow.
- **No metric observes forced-JWKS-refresh churn** — pairs with the existing observability tail; worth monitoring if kid-rotation frequency becomes operationally significant.
