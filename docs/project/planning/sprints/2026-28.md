# Sprint 2026-28 — CMS Fraud Workbench: All Analytics Live + Demo

> Owned by the Project Manager agent.
> Sprint: **2026-28** · Dates: **2026-07-16 → 2026-07-29** · Capacity: **25 SP** · Theme: **every analytic live and visible on CMS billing data — GNN/clustering wired to the real graph, explainability engine (SHAP + LLM narratives), IntegrityAI-inspired workbench reshape, and a scripted, mechanically-validated full-capability demo**

## Sprint inputs (owner-confirmed 2026-07-16)

- **sprint-id**: `2026-28` — sequential after 2026-27 (delivered + fix-later burn-down merged 2026-07-16, PR #27).
- **dates**: 2026-07-16 → 2026-07-29, starting immediately per the owner's "one initiative, start now" ruling.
- **focus**: owner directive 2026-07-16 — "shipping the full feature fraud detection/graph-visualization/investigative analytics workbench for CMS billing. Prioritize having all analytics live and visible including GNN integration, clustering, policy/explainability engine enabling a complete demo of all capabilities."
- **design note**: `docs/superpowers/specs/2026-07-16-sprint28-cms-fraud-workbench-design.md` (approved 2026-07-16) — records the verified predecessor state, the five owner rulings (staged GNN, 1% TN scripted demo, IntegrityAI-inspired reshape, SHAP + LLM explainability, two-track delivery), and the story architecture.

## Goal

A complete, repeatable, scripted demo of every platform capability on real CMS DE-SynPUF data: ingest → graph → live clustering/link prediction → peer/timeseries anomalies → risk → alerts → evidence packs with SHAP attribution and LLM narratives → policy intelligence → RAG chat — presented through a reshaped investigator workbench worthy of the demo.

## Verified predecessor state (code-checked survey, 2026-07-16)

Real and wired today: peerstats z-scores → `entity_derived_signals` → risk scoring → monitoring alerts → evidence packs (factor+subgraph); policy rule engine (ingest-triggered); GraphCanvas mounted in the live workbench; dashboard on live queries; DE-SynPUF/NPPES TN subset staged with 1% and full-scale ingest scripts. Gaps: GNN stage no-ops (both factories hard-code an empty `InMemoryGraphSnapshotSource`; `/analytics/gnn/clusters` always empty); timeseries anomalies API-only; explainability narrative is template joining and SHAP unwired; new analytics have no UI surface; no validated demo script. Several `docs/backlog/*.md` module stories are stale vs. code (frontend.01, monitoring.02, frontend.05) — closeout hygiene item.

## Committed (two-track)

| ID | Story | Track | Priority | Est. | Acceptance summary | Depends on |
|---|---|---|---|---|---|---|
| **BL-046 (B1)** | GNN live: repository-backed snapshot source, bounded caps, cluster persistence + cascade | backend | P1 | 5 SP | Pipeline GNN stage runs on real graph data; `GET /analytics/gnn/clusters` serves real communities; `community_id`/`centrality_score` on entities; cluster store in KB-delete cascade; live-verified on 1% TN. Plan: `docs/superpowers/plans/2026-07-16-sprint28-b1-gnn-live.md`. | — |
| **BL-047 (B2)** | Timeseries anomaly detection into the ingest pipeline (analytics.06/07) | backend | P1 | 3 SP | Ingest-triggered anomaly stage after peerstats; anomalies persisted + feeding monitoring; workbench chart shows pipeline-produced anomalies. Spec+plan at story start. | B1 merged |
| **BL-048 (B3)** | Explainability engine: LLM narratives (analytics.13) + production SHAP (analytics.14) | backend | P1 | 5 SP | LLM-backed evidence narrative with template fallback; SHAP attributing `LinearScoringStrategy` over `entity_derived_signals`; attribution section in evidence packs. Spec+plan at story start. | B2 merged |
| **BL-049 (U1)** | Workbench reshape design pass (IntegrityAI-inspired, design-only) | UI | P1 | 2 SP | Approved design doc + annotated mockups: visual direction, per-page layouts (workbench/dashboard/alerts), component inventory; fully domain-config-driven (housing pack must render). No code. | — (parallel) |
| **BL-050 (U2)** | Reshape + analytics surfacing implementation | UI | P1 | 8 SP | U1 design executed; cluster overlays + membership on GraphCanvas; dashed predicted links w/ confidence; SHAP bars + LLM narrative in evidence viewer; policy items on entity/alert views; anomaly markers; dashboard clusters live; Playwright e2e per capability. | U1 approved; consumes B1–B3 APIs as they land |
| **BL-051 (D1)** | Complete demo: presenter script, `make demo-cms`, demo-tuned fraud rule packs, e2e-validated walkthrough | demo | P1 | 3 SP | Script at `docs/demo/` covering every capability on 1% TN; `make demo-cms` orchestration; 2–3 realistic CMS fraud policy packs (config only); full-stack e2e validating the walkthrough. | B1–B3 + U2 |
| | **Committed core total** | | | **26 SP** | (1 SP over nominal — accepted: recent sprints delivered ahead of window) | |

## Stretch (strictly after D1 is demo-ready)

| ID | Story | Est. | Notes |
|---|---|---|---|
| **BL-052 (S1)** | Trained GNN (BL-030 pulled forward): PyTorch Geometric behind existing protocols, `[torch-geometric]` extra, artifact persistence (analytics.02), config-selected | 8 SP | Heuristic engine stays the default; the demo never depends on S1. |

## Sequencing

Backend track B1 → B2 → B3 (sequential, each spec+plan+SDD per cadence). UI track U1 immediately in parallel; U2 starts on U1 approval and integrates each backend API as it merges. D1 gates on both tracks. S1 strictly after D1.

## Definition of done (per story + sprint)

Unchanged repo gates: pyright strict 0 errors, ≥85% coverage, ruff clean, contract regen + `npm run build` on any frontend-consumed model change, browser verification for UI work, live verification against `make dev`, full-stack Playwright e2e for the demo path. Sprint DoD: D1's scripted walkthrough runs green end-to-end on a fresh `make dev` + `make demo-cms`.

## Progress (2026-07-18)

- **BL-047 (B2) — done (backend implementation).** All 8 implementation/docs
  tasks landed on `feat/sprint-2026-28-b2-timeseries-anomalies`: config
  surface (`TimeseriesMetricSpec`/`TimeseriesAnalyticsConfig` with
  cross-reference validation, Task 1); the anomaly store (protocol +
  in-memory/Postgres adapters, migration `0011_timeseries_anomalies.py`,
  Task 2); the record-aggregate per-entity series source over
  `raw_records` (`RecordAggregateTimeSeriesSource`, reusing the peerstats
  `RecordColumnSourceProtocol` SQL, Task 3); the worker stage
  `run_timeseries_stage` (`agent/coordinator.py`), gated on
  `capabilities.timeseries`, running best-effort and independently of
  peerstats on every `RecordsIngestedEvent` (Task 4); the KB-delete cascade
  step `timeseries_anomalies` (Task 5); the `GET
  /analytics/timeseries/{entity_id}` route rewrite off the seeded `ApiState`
  path onto real record-aggregate series + persisted anomalies (Task 6); CMS
  DE-SynPUF and Air Force housing pack config additions (`peer_stats` +
  `timeseries` blocks, Task 7); and full gates + docs/backlog reconciliation
  (Task 8, this pass) — including closing analytics.07's previously-deferred
  request-level Postgres integration-test AC and dropping analytics.06 in
  favor of the record-aggregate series source (see
  `docs/backlog/analytics.md` for the full rationale). Backend gates green
  in-session: `make test` (2612 passed, 5 skipped, 97% coverage), `pyright`
  (0 errors), `ruff check --no-cache .` (clean). Live full-stack verification
  landed as Task 9 of the same branch (see the 2026-07-19 entry below); D1's
  scripted, mechanically-validated demo walkthrough remains the separate
  sprint-exit gate.

## Progress (2026-07-19 — B2 live verification, Task 9)

- **BL-047 (B2) — live-verified.** Task 9's full-stack pass ran the worker
  timeseries stage end-to-end against the TN 1% CMS demo KB (`82db11c3`) on
  both the carrier and inpatient feeds: `timeseries_anomalies` rows and
  `entity_derived_signals` `timeseries_anomaly:*` rows were persisted
  alongside peerstats signals with zero stage warnings/errors in worker logs;
  `provider:1003195173`'s risk profile carries both signal families via the
  API; the workbench entity-detail view renders the anomaly chart plus an
  anomaly chip and both risk-factor families with zero console errors
  (screenshot `b2-task9-workbench-anomaly-chip.png`); a scratch KB's
  KB-delete cascade purged its anomaly and signal rows to zero. The pass
  surfaced and fixed four defects, all landed on
  `feat/sprint-2026-28-b2-timeseries-anomalies`: `30c1a9a` (peerstats
  skip-on-missing-`time_column`), `f54d5a9` (serialize TN demo uploads
  against the per-KB workflow lock), `00f7fa8` (Qdrant adapter chunks
  upserts past its 32MB payload cap), and `42ef186` (risk detail route
  migrated off seeded `ApiState` onto the DI risk service — closes
  `docs/backlog/analytics.md` story analytics.28). B2's backend slice is now
  both code/gate-verified and live-verified; **D1's scripted, mechanically-
  validated demo walkthrough is the only piece of B2's original "not yet
  done" note still owed**, and remains scoped to D1 per the two-track
  sequencing above. `docs/project/planning/backlog.md` BL-047 row reflects
  this distinction.

## Closeout hygiene (carried into D1/closeout)

- Correct stale module-story "Current State" texts: frontend.01, monitoring.02, frontend.05 (survey 2026-07-16).
- Reconcile `docs/backlog/analytics.md` stories delivered by B1–B3 (analytics.03/.05/.06/.07/.13/.14, partial .04/.24) and `docs/project/planning/backlog.md` rows BL-046..052.
